@model IEnumerable<EstadosPedido>

@{
    var filtroNombre = Context.Request.Query["nombre"].ToString();
    var estatus = Context.Request.Query["estatus"].ToString();


    var hayFiltrosActivos =
       !string.IsNullOrWhiteSpace(filtroNombre) ||
       !string.IsNullOrWhiteSpace(estatus);
}

@section Styles {
    <link rel="stylesheet" href="~/css/admin/pages/estadosPedidos/index.css" asp-append-version="true" />
}


<!--HERO-->
<div class="container admin-index-page estPedidos-index">
    <div class="card admin-hero-card">
        <div class="card-body d-flex justify-content-between align-items-center">
            <div>
                <h1 class="admin-hero-title">
                    <i class="bi bi-flag me-2"></i>Estados de Pedido
                </h1>
                <p class="admin-hero-subtitle">
                    Administra los estados disponibles para los pedidos en el sistema.
                </p>
            </div>

            <div class="d-flex gap-2 flex-wrap">
                <button class="btn btn-outline-light admin-btn-main"
                        type="button"
                        data-bs-toggle="collapse"
                        data-bs-target="#filtrosBusqueda"
                        aria-expanded="@(hayFiltrosActivos ? "true" : "false")"
                        aria-controls="filtrosBusqueda">
                    <i class="bi bi-funnel me-1"></i> Filtros
                </button>

                <a asp-action="Create" class="btn btn-light admin-btn-main">
                    <i class="bi bi-plus-lg me-1"></i> Nuevo estado
                </a>
            </div>
        </div>
    </div>


    <!-- FILTRO -->
    <div class="collapse @(hayFiltrosActivos ? "show" : "")" id="filtrosBusqueda">
    <div class="card admin-filter-card mt-3">
        <div class="card-body">
            <form method="get">
                <div class="row g-3 align-items-end">

                     <!-- NOMBRE -->
                    <div class="col-12 col-md-6 col-xl-3">
                        <label class="form-label admin-filter-label">Nombre</label>
                        <div class="input-group shadow-sm admin-filter-group">
                            <span class="input-group-text bg-white">
                                <i class="bi bi-tag text-primary"></i>
                            </span>
                            <input type="text"
                                   name="nombre"
                                   class="form-control"
                                   placeholder="Nombre del estado..."
                                   value="@filtroNombre" />
                        </div>
                    </div>

                        <!-- ESTATUS -->
                        <div class="col-12 col-md-6 col-xl-3">
                            <label class="form-label admin-filter-label">Estado</label>
                            <div class="input-group shadow-sm admin-filter-group">
                                <span class="input-group-text bg-white">
                                    <i class="bi bi-toggle-on text-primary"></i>
                                </span>
                                <select name="estatus" class="form-select">
                                    <option value="">Todos</option>
                                    <option value="1" selected="@(estatus == "1")">Activo</option>
                                    <option value="0" selected="@(estatus == "0")">Inactivo</option>
                                </select>
                            </div>
                        </div>

                        <!-- BOTONES -->
                        <div class="col-12">
                            <div class="admin-filter-actions">
                                <button type="submit" class="btn btn-primary shadow-sm">
                                    <i class="bi bi-search me-2"></i>Buscar
                                </button>

                                <a asp-action="Index" class="btn btn-outline-secondary shadow-sm">
                                    <i class="bi bi-arrow-clockwise me-2"></i>Limpiar
                                </a>
                            </div>
                        </div>

                    </div>
                </form>
            </div>
        </div>
    </div>

     <!-- TABLA -->
    <div class="card admin-table-card mt-3">
        <div class="table-responsive">
            <table class="table table-hover align-middle admin-table jspaginacion">
                <thead>
                    <tr>
                        <th class="est-ped-col-nombre">Nombre</th>
@*                         <th class="est-ped-col-descripcion">Descripción</th> *@                     
                        <th class="est-ped-col-estado text-center">Estado</th>
                        <th class="est-ped-col-creacion">Creación</th>
                        <th class="est-ped-col-acciones">Acciones</th>
                    </tr>
                </thead>

                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="est-ped-col-nombre">
                                    @item.Nombre
                                </td>

@*                                 <td class="est-ped-col-descripcion">
                                    @(item.Descripcion != null && item.Descripcion.Length > 30
                                        ? item.Descripcion.Substring(0, 30) + "..."
                                        : item.Descripcion)
                                </td> *@

                                <td class="est-ped-col-estado text-center">
                                    @if (item.Estatus == 1)
                                    {
                                        <span class="admin-status-badge admin-status-active">
                                            Activo
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="admin-status-badge admin-status-inactive">
                                            Inactivo
                                        </span>
                                    }
                                </td>

                                <td class="est-ped-col-creacion">
                                    @item.FechaCreacion.ToShortDateString()
                                </td>

                                <td class="est-ped-col-acciones">
                                    <div class="btn-group">
                                        <a asp-action="Edit" asp-route-id="@item.Id"
                                               class="btn admin-action-btn admin-btn-edit">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <a asp-action="Details" asp-route-id="@item.Id"
                                               class="btn admin-action-btn admin-btn-view">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                   
                </tbody>
            </table>
        </div>

        <div class="admin-card-footer">
            Total: <strong>@(Model?.Count() ?? 0)</strong> estados
        </div>
    </div>

</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_Paginacion");
    }
}